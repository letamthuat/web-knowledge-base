# LOCALHOST_NOTES — Module Học tập (Study)

## Bối cảnh & ý định (PM/BA)
*Cập nhật: 2026-07-15*

- User học bằng cách upload handbook (MD/PDF) lên web rồi đọc — hiện "chỉ đọc, suy ngẫm rồi thôi", muốn **học sâu và học kỹ**
- **User KHÔNG dùng highlight** → mọi tính năng phải lấy nguyên liệu từ `extractedText` + AI, không dựa vào highlight
- Nhịp học thực tế: đọc **1-3 tiểu mục** rồi mới ôn/giảng lại — không ôn theo cả module (dài quá)
- Feynman: user muốn **nói** (thu âm) thay vì gõ — gõ chỉ là fallback
- Lo ngại quota Gemini free tier → đã tính: ~10 call/ngày học vs trần ~250, an toàn; mọi hoạt động ôn chạy local
- Học liệu lấy từ **handbook HOẶC tài liệu lẻ** → khái niệm Study Space với nguồn gắn linh hoạt

## Quyết định thiết kế đã chốt
*Cập nhật: 2026-07-15*

- Scope AI mặc định = **checkpoint tự động** ("từ lần ôn trước tới chỗ đang đọc"), có nút chỉnh trước khi gửi
- Handbook lớn → materialize cây thành **study units**, thứ tự học = thứ tự cây, trạng thái ⬜🔵🟡🟢🔴; unit 🔴 = "phai" khi lâu không ôn
- **Không khóa việc đọc, không trừng phạt** khi bỏ bài — chỉ làm hậu quả nhìn thấy được; "Chế độ nghiêm" (khóa unit kế) là flag để sau
- Streak **chỉ tính hành động chủ động** (quiz/Feynman/ôn card), không tính mở-ra-đọc
- Quiz cấu trúc **3 MCQ vận dụng + 2 tự luận ngắn**, mọi câu kèm trích đoạn gốc (chống hallucinate + click nhảy về tài liệu)
- Flashcard 3 loại: khái niệm / vận dụng / liên kết; user duyệt từng card trước khi lưu; interval nhân đôi + 2 nút Quên/Nhớ (chưa SM-2, nhưng ghi `review_logs` từ đầu)
- **Mật độ card (user chốt 16/07):** tiểu mục của handbook này rất dày (4 tầng, ~43k ký tự) → AI sinh **8-12 card/tiểu mục**, phủ đủ 4 tầng nội dung (trực giác → công thức → giải số → giới hạn/nhân quả) × 3 loại card; 1-2 card/tiểu mục là KHÔNG đủ. Thư viện card (tất cả) tách khỏi queue "đến hạn hôm nay" (subset theo lịch). Seed mẫu chuẩn: 2.1.3 có bộ đầy đủ 10 card — dùng làm few-shot cho prompt sinh card.
- Tracking đầy đủ ngay v1: `quiz_attempts`, `review_logs`, `study_sessions` — "không đo thì không biết chỗ nào yếu"
- Tên sidebar tạm: **"Học tập"**; space demo: **"Ôn tập Aptis"**

## Kế hoạch học tập (user mô tả 16/07)
*Cập nhật: 2026-07-16*

- **Luồng:** nạp handbook xong → hệ thống ĐÁNH GIÁ khối lượng → KHUYẾN NGHỊ học bao nhiêu ngày, mỗi ngày bao nhiêu giờ → user tăng/giảm số ngày thì TÍNH LẠI giờ/ngày (live) để user tự quyết → user chọn các thứ trong tuần học được (vd chỉ T2-T4-T6 + cuối tuần) → chốt → sinh plan chi tiết.
- **Plan chi tiết theo ngày:** ngày nào đọc mục gì, làm bài tập gì, quiz gì, flashcard gì, giảng (Feynman) mục gì — bám checklist 4 việc/tiểu mục + xen kẽ slot ôn card đến hạn.
- **Nguyên tắc rule-first (user nhấn mạnh):** chỗ nào làm thành RULE được thì phải là rule, KHÔNG gọi Gemini — vì API free tier không đảm bảo (quota/lỗi). Gemini chỉ dành cho sinh nội dung học (quiz/card/chấm Feynman) như đã chốt on-demand. → Đánh giá khối lượng + khuyến nghị ngày-giờ + xếp lịch = 100% rule/thuật toán deterministic (đếm tiểu mục, độ dài extractedText → thời gian đọc + overhead cố định mỗi hoạt động; bin-packing theo thứ tự cây vào các ngày user rảnh).
- **Định mức rule prototype (16/07, chờ user chốt khi xem UI):** đọc 600 ký tự/phút; quiz 10′, tạo card 12′, Feynman 8′, trạm tổng kết 15′/mục; slot "ôn flashcard đến hạn" 10′ chèn đầu MỖI ngày học từ ngày 2. Khuyến nghị mặc định quy về ~60′/ngày → suy ra số ngày. Unit 🔴 decayed chỉ xếp lại quiz (khớp banner checklist). Wizard mặc định tick module đang học dở; module chưa bung tiểu mục ước lượng = dung lượng ÷ 43k ký tự/tiểu mục, gắn nhãn "(ước lượng — bung chi tiết khi nạp)". Task đọc được tách phiên (tối thiểu 15′), task khác nguyên khối. Số liệu smoke-test: M2 còn lại ≈ 33g50 → 41 ngày × ~60′; cả handbook ≈ +133h từ 5 module coarse.
- **Prototype đã dựng (16/07):** tab thứ 5 "Kế hoạch" (`PlanTab.tsx` + rule engine `plan.ts`): bước 1 bảng khối lượng theo module (tick chọn), bước 2 stepper số ngày ↔ giờ/ngày tính lại live + chip chọn T2–CN + ngày xong dự kiến + cảnh báo nặng/nhẹ → sinh lịch gom theo tuần (collapse), mỗi ngày là list task có icon/phút.
- **Redesign sidebar toàn app (user chốt 16/07, chỉnh lần 2):** cả 6 MODULE là MỘT KHỐI CÙNG CẤP trên cùng sidebar (không tách trên/giữa/dưới): ① Tìm kiếm (mở modal TÌM KIẾM TOÀN CỤC: tên tài liệu + nội dung + ghi chú) ② Học tập ③ Thư viện ④ Ghi chú ⑤ Cài đặt ⑥ Thùng rác — style NavRow đồng nhất, active theo panel. CÂY THƯ VIỆN nằm LỒNG TRONG mục Thư viện (chỉnh lần 3, user chốt): hàng "Thư viện" có chevron phải bung/thu — click label = điều hướng /library + tự bung, click chevron = chỉ bung/thu; con thụt lề border-trái gồm ô "Lọc theo tên" + 2 nhánh cùng cấp "Handbook" (chứa Domain, nút + tạo Domain trên hàng, hiện khi hover) và "Tài liệu lẻ". **Học tập & Ghi chú cũng bung/thu (chỉnh lần 4, user chốt):** cùng pattern ExpandableNavRow — Học tập bung ra danh sách không gian học (emoji + tên + badge card đến hạn, click = deep-link /study/:spaceId); Ghi chú bung ra ~10 ghi chú gần nhất (tiêu đề hoặc trích body, click = /notes?note=:id — NotesPageInner hỗ trợ sẵn query này) + "Xem tất cả (N)"; **Mặc định khi mới vào: THU HẾT toàn bộ** (chỉnh lần 5, user chốt) — Học tập/Thư viện/Ghi chú/nhóm Handbook/Domain đều thu gọn, bung là hành động chủ động; subscription notes chỉ chạy khi bung (render có điều kiện). **Bỏ header desktop + toggle vào sidebar (chỉnh lần 7, user chốt):** header các trang Library/Notes/Trash ẩn hoàn toàn trên ≥xl (mobile giữ nguyên — vẫn cần menu/brand/search/logout); nút ẩn sidebar (PanelLeftClose) nằm trên hàng brand trong sidebar; khi sidebar ẩn → nút nổi PanelLeftOpen cố định góc trên-trái là lối mở lại duy nhất. Mục "Tìm kiếm" sidebar nối vào SearchModal THẬT sẵn có (tài liệu + ghi chú + highlight, hook useSearch) — mock GlobalSearchModal đã xóa, KHÔNG cần xây màn search mới; header reader giữ (chứa action theo tài liệu).
- **Brand + tài khoản vào sidebar (chỉnh lần 6, user chốt, tham khảo sidebar MDLZ):** đầu sidebar = logo + "Web Knowledge Base" + tagline; đáy sidebar = avatar chữ cái + email + nút Đăng xuất. Header các trang (Library/Notes/Trash): logo/tên/email/nút đăng xuất ẨN trên desktop ≥xl (đã có trong sidebar), GIỮ trên mobile vì mobile không có sidebar. Màn search toàn cục là tính năng mới — prototype mock trước, BE cần full-text search (Postgres tsvector trên extractedText + notes).
- **Redesign picker chọn tiểu mục Feynman (user chê 16/07 "chưa tối ưu"):** flat list cũ trộn M1 với 2.x.x, không nhóm/lọc/trạng thái — không dùng nổi khi 29+ mục đã đọc. Picker mới: ① nhóm theo module (mục cấp module đứng riêng không heading); ② ô lọc theo tên HOẶC số mục (gõ "2.2"); ③ chấm màu trạng thái học trên từng dòng (cùng ngôn ngữ cây lộ trình); ④ đếm X/4 đậm + cảnh báo khi đủ; ⑤ chip đã chọn có nút × bỏ nhanh; ⑥ nút "↺ phần vừa đọc" reset về scope checkpoint; ⑦ danh sách cuộn max-height thay vì kéo dài trang. Vẫn giữ: chỉ hiện mục readPct>0, tối đa 4, disabled khi đủ.
- **Giờ nhắc user chốt (16/07): 08:45 và 21:15** — 2 push/ngày. Ngữ nghĩa theo rule chọn nội dung theo giờ: mốc sáng (08:45) = tóm tắt việc hôm nay (buổi học + X card đến hạn); mốc tối (21:15) = nhắc vào học / còn gì chưa xong của buổi hôm nay. User đổi/thêm giờ được (mock UI trong tab Kế hoạch, mặc định 2 mốc trên). Spec BE: bảng `notification_settings` (user_id, enabled, times[], types, timezone). **Timezone user chốt: UTC+7 (Asia/Ho_Chi_Minh)** — 08:45/21:15 hiểu theo giờ VN; pg_cron chạy theo UTC nên job so khớp `times[]` sau khi quy đổi từ cột `timezone` (mặc định +7, không hardcode để sau này vẫn mở rộng được).
- **Push notification PWA (user hỏi 16/07 — khả thi, $0):** Web Push chuẩn + VAPID tự sinh (không cần Firebase/dịch vụ trả phí). Kiến trúc: ① client xin quyền + subscribe → bảng `push_subscriptions` (mỗi thiết bị 1 dòng); ② thêm listener `push` + `notificationclick` vào sw.js sẵn có — click mở deep-link `/study/...` (URL scheme vừa làm là tiền đề); ③ gửi = Supabase Edge Function + pg_cron (đều free tier) chạy 15'/lần, nội dung nhắc 100% rule từ plan (giờ học buổi hôm nay, X card đến hạn, buổi dở, streak sắp đứt) — 0 call Gemini. Caveat iOS: chỉ nhận push khi PWA đã Add to Home Screen + iOS ≥16.4; Android/desktop đầy đủ. Thuộc Phase backend; phần mock được trước = màn cài đặt nhắc (bật/tắt, giờ nhắc, loại nhắc) để chốt UX.
- **Deep-link URL cho module Học tập (user yêu cầu 16/07):** URL phải phản ánh đúng màn hình — scheme: `/study` (danh sách) · `/study/:spaceId` (Tổng quan) · `/study/:spaceId/:tab` (plan|review|quiz|feynman). Đã làm ở prototype: route Next đổi thành optional catch-all `app/study/[[...slug]]/page.tsx` (hard-reload/bookmark deep-link được); StudyPageInner sync 2 chiều qua `usePathname` (Next 15 patch history.pushState) — điều hướng nội bộ pushState, URL đổi từ ngoài (back/forward, sidebar, deep-link) thì state theo URL; spaceId/tab không hợp lệ tự rơi về `/study`/Tổng quan. focusCtx (highlight tiểu mục) là state tạm KHÔNG nằm trên URL — chủ đích, vì chỉ có nghĩa trong luồng điều phối. Spec BE: giữ nguyên scheme này, spaceId là id thật từ DB.
- **Redesign màn danh sách không gian học (user chê 16/07 "mới vào mà xấu, hẹp hơn màn trong"):** đã làm lại — container đồng bộ độ rộng với màn trong (max-w-4xl → 1400px từ 2xl); thêm 4 stat tile gộp toàn bộ space (chuỗi 🔥 / card đến hạn / phút hôm nay / tiểu mục vững — số dùng token chữ, icon mang màu ngữ nghĩa); grid card 1→2→3 cột; card nâng cấp (emoji tile nền primary/10, progress bar dày hơn + % đậm, hover nhấc nhẹ + "Vào học →"); Spec BE: 4 chỉ số gộp tính từ tracking tables, giá trị theo NGÀY hiện tại. Ghost card "Tạo không gian học" đã thêm rồi BỎ (user chốt 16/07: trùng với nút hero — chỉ giữ 1 lối tạo là nút "+ Không gian học" trên cùng).
- **Lưu plan khi có BE (user xác nhận 16/07):** bấm "Tạo kế hoạch chi tiết" = LƯU SNAPSHOT plan vào DB, KHÔNG regenerate mỗi lần mở (đầu vào rule đổi theo tiến độ học → regenerate làm lịch tự trôi, mất tính cam kết; PWA/laptop phải thấy cùng 1 lịch). Schema: `study_plans` (1 active/space: config wizard + ngày bắt đầu/kết thúc dự kiến) + `study_plan_tasks` (snapshot từng việc: date, seq, type, unit_key, minutes, label). "Xếp lại lịch"/tạo lại = plan mới active, bản cũ archived (append-only). Done-state task KHÔNG lưu tay — suy từ hành động học thật (readPct, quiz_attempts, card, phiên Feynman) qua join; tick tay chỉ là mock prototype. Tạo/xếp lại plan = 0 call Gemini kể cả ở BE.
- **Review 3 khối rail HÔM NAY / CẦN HỌC LẠI / HOẠT ĐỘNG (user yêu cầu 16/07) — rule chạy thật, 0 call AI:**
  - *HÔM NAY* (đang seed tĩnh): nguồn ưu tiên ① việc buổi hôm nay trong plan chưa xong ② nợ carry-over buổi đã qua ③ ôn X card đến hạn (luôn hiện khi X>0) ④ chưa có plan → fallback "Đọc tiếp" unit 🔵 + remediation 🔴. DEDUP bắt buộc: 1 việc không được xuất hiện 2 nguồn (hiện "quiz 2.1.3" trùng ở HÔM NAY + CẦN HỌC LẠI + Buổi 1 plan). Cap 4-5 mục.
  - *CẦN HỌC LẠI*: công thức vào danh sách = unit 🔴 decay (>14 ngày không hoạt động chủ động sau khi đạt 🟢) HOẶC quiz best <80% (≥1 lần làm) HOẶC card Quên ≥3 lần/30 ngày — tính từ `quiz_attempts`/`review_logs`/`study_sessions`; reason = template + số thật; sort theo điểm rủi ro; cap 3; ra khỏi danh sách khi quiz lại ≥80%. Đã sửa prototype: nút "Học lại" (trước đây chết) giờ điều phối sang tab Kiểm tra đúng section.
  - *HOẠT ĐỘNG 12 TUẦN* (heatmap đang sóng sin giả): mức 0-4 theo phút học chủ động/ngày từ `study_sessions` (0 · 1-10′ · 11-25′ · 26-45′ · >45′). **Streak khi có plan chỉ tính trên ngày-học-theo-lịch** — ngày ngoài weekday đã chọn là trung tính, không đứt chuỗi (nghỉ đúng kế hoạch không bị phạt); chưa có plan thì tính mọi ngày.
- **Việc dở KHÔNG auto-trượt sang ngày sau (user hỏi 16/07, đã giải thích & đồng thuận hướng):** lịch giữ nguyên khi bỏ buổi — buổi trễ viền vàng + panel đếm "N buổi chưa hoàn thành"; chia lại là hành động chủ động qua nút "Xếp lại lịch" (dồn việc chưa xong, chia từ ngày mai, hiện ngày xong mới). Lý do: auto-trượt làm ngày sau âm thầm phình + ngày kết thúc trôi vô hình — trái nguyên tắc "hậu quả nhìn thấy được, không trừng phạt"; xếp lại là quyết định của user (tăng giờ/ngày hay lùi ngày xong). Spec bản thật: khối "HÔM NAY" phải tự gom việc dở của các buổi đã qua (carry-over hiển thị, kiểu "còn nợ Quiz 2.2.3 từ hôm qua") dù lịch không đổi — prototype chưa mô phỏng vì todayMenu là seed tĩnh.
- **Plan chỉ xếp việc còn thiếu + nhãn tự giải thích (user hỏi 16/07):** user thấy "Quiz 2.1.3" mà không có "Đọc 2.1.3" tưởng seed sai — thực ra unit đã đọc 100% từ trước nên plan chỉ xếp việc dở (đúng thiết kế). Fix UX: nhãn quiz remediation phải nói lý do — "Quiz lại X (🔴 lâu không ôn)" / "Quiz lại X (mới 72%, cần ≥80%)" vs "Quiz X" lần đầu. Nguyên tắc chung cho spec: task trong plan phải tự giải thích vì sao nó tồn tại.
- **Task trong plan phải điều hướng như Tổng quan (user chốt 16/07):** click tên việc trong card Buổi = mở đúng nơi tương ứng theo cơ chế điều phối ngữ cảnh — Đọc → mở reader đúng tài liệu; Quiz → tab Kiểm tra highlight đúng section; Tạo flashcard → tab Ôn tập đúng bộ card; Giảng → tab Feynman scope đúng tiểu mục; Ôn card → tab Ôn tập. Tick hoàn thành tách riêng = bấm chấm tròn/checkbox đầu dòng. Task thuộc module coarse (chưa nạp) → thông báo chưa nạp.
- **Benchmark Prep.vn (user đưa 16/07):** user thấy UI lịch học TOEIC của Prep đẹp hơn — điểm học được: ① mỗi ngày học = CARD "Buổi N" xếp GRID nhiều cột (tận dụng ngang) thay list dọc, có separator theo tháng; ② trạng thái hoàn thành đậm nét: chip xanh ✓, đếm 9/9 mỗi buổi; ③ panel "Tiến độ học": số ngày còn lại, hoàn-thành-vs-kế-hoạch, cảnh báo "đang chậm hơn kế hoạch X%", đếm "N buổi chưa hoàn thành"; ④ nút "Xếp lại lịch" (reschedule) — xác nhận nhu cầu giãn lịch khi bận đã nêu; ⑤ filter "Hôm nay" riêng. → Đã áp vào prototype: lịch chuyển thành grid card buổi học theo tháng, tick hoàn thành từng việc (mock), thanh tiến độ + đếm buổi trễ + nút "Xếp lại lịch" (dồn việc chưa xong, xếp lại từ hôm nay bằng rule — 0 call AI).

## Quy trình triển khai (user đặt)
*Cập nhật: 2026-07-15*

1. PRD mỏng (`PRD.md` cùng thư mục) ✅
2. **UI prototype + seed data** để user view giao diện ← đang ở bước này
3. User chốt giao diện → viết spec chi tiết
4. Backend (nhánh `supabase`, schema mới sinh ở kiến trúc đích)

## Quan sát khi prototype
*Cập nhật: 2026-07-16*

- **UI co cụm giữa màn hình (user quan sát 16/07):** các tính năng dựng tạm thấy ổn về chức năng nhưng cần cải thiện UI — không gian hiển thị rộng (màn ~1920px) mà nội dung bị bó trong cột `max-w-4xl` (896px) căn giữa tại `StudyPageInner.tsx`. Đã chốt: layout 2 cột cho tab Tổng quan trên màn rộng (lộ trình = cột chính, action cards + streak + chú giải = side rail sticky ~340px), các tab lịch sử giữ 1 cột.
- **Đa thiết bị (user chốt 16/07):** user sẽ học cả trên PWA (mobile) — mọi cải thiện UX/UI module Học tập phải responsive 3 tầng: mobile PWA (1 cột, hit-target lớn, chừa bottom nav) → laptop ~1366px (1 cột như hiện tại) → desktop rộng ≥ xl (2 cột). Không thiết kế riêng cho desktop rồi vá mobile.

- **Granularity tiểu mục = cấp x.y.z (heading `###`, vd 2.1.3)** — user chốt: cấp x.y (`##`, vd 2.1) quá nhiều nội dung cho 1 lần ôn/giảng. Lộ trình vì vậy có 3 tầng: file handbook → mục x.y → tiểu mục x.y.z; đơn vị scope cho quiz/Feynman/flashcard là x.y.z.
- Handbook thật để test: **"Supply Chain 360 — The four lens handbook"** (7 module content: M1 chiến lược-rủi ro, M2 demand planning, M3 supply planning MPC, M4 inventory, M6 warehouse, M7 transportation, M8 finance + glossary/mục lục). Doc chuẩn granularity: `02-demand-planning.md` (~1MB markdown, 23 tiểu mục x.y.z, mỗi tiểu mục có 4 tầng `####` theo phương pháp 4-tier).
- Seed data đã dựng theo nội dung thật của M2 (ADI–CV², SES, Croston, damped trend, unconstrained demand…) — dùng làm mẫu few-shot cho prompt Gemini khi làm backend.
- Lưu ý cho spec: heading `####` (x.y.z.w — 4 tầng trong 1 tiểu mục) KHÔNG phải đơn vị ôn; parser section dừng ở `###`.
- **Luồng lộ trình ↔ reader (user hỏi 15/07):** click tiểu mục trên lộ trình = MỞ tài liệu trong reader (spec: scroll đúng heading x.y.z — MarkdownViewer đã có rehype-slug + scroll-to-heading sẵn). Chiều ngược: đọc trong reader → nút nổi "Học phần này" → quiz/Feynman scope checkpoint. Prototype đã nối chiều 1 (mở doc, chưa scroll heading); chiều 2 (nút nổi trong reader) chưa mock.
- **3 tab = trình quản lý LỊCH SỬ theo lộ trình (user chốt 16/07):** Ôn tập / Kiểm tra / Feynman đều tổ chức nội dung theo cây module → tiểu mục giống Tổng quan, và CHỈ chứa lịch sử (card đã tạo, quiz đã làm, phiên đã giảng) — tiểu mục chưa học/chưa làm KHÔNG xuất hiện. Khởi tạo hành động mới (làm quiz lần đầu, tạo card, giảng lần đầu) luôn đi từ checklist tiểu mục ở Tổng quan. Ôn tập: nút "Ôn hôm nay (X card đến hạn)" trên cùng + thư viện card theo tiểu mục (bung xem từng card, "Ôn bộ này"). Kiểm tra: section nhóm theo module, thêm section chưa-có-lịch-sử CHỈ khi được điều phối tới. Feynman: nhật ký nhóm module → tiểu mục.
- **Feynman 2 chế độ (user chốt 16/07):** ① Từ checklist ở Tổng quan = giảng ĐÚNG 1 tiểu mục bài học yêu cầu — scope khóa, không có picker (có link thoát sang chế độ tự do). ② Vào thẳng tab Feynman = giảng TỰ DO nhiều tiểu mục hoặc cross-module (tối đa 4 — user xác nhận giữ 16/07; lý do: rubric sâu + ~11-15k token/tiểu mục, 4 mục ≈ 45-60k token/call an toàn với TPM free tier; nhu cầu "cả module" đi qua Trạm tổng kết dùng tóm tắt) để thể hiện kiến thức liên kết; chọn ≥2 mục thì AI chấm thêm phần "kết nối kiến thức". Nhật ký tách nhóm riêng "🔗 Phiên liên kết" (đánh dấu bằng "↔" trong scopeLabel) khỏi các phiên theo tiểu mục đơn. Spec prompt: chế độ liên kết đưa text của N tiểu mục + yêu cầu rubric thêm mục connection.
- **Scope Feynman phải tường minh (user góp ý 16/07):** khối thu âm hiển thị rõ "SẼ GIẢNG THEO N TIỂU MỤC" + chip tên từng tiểu mục (không dùng câu mô tả mơ hồ "từ lần ôn trước → …"). Nút "chỉnh" mở picker checkbox: chỉ liệt kê tiểu mục ĐÃ ĐỌC (readPct > 0), tối đa 3 tiểu mục/lần giảng. Mặc định = tính từ checkpoint (phần đọc từ lần ôn trước); được điều phối từ checklist thì = đúng tiểu mục đó. Nút 🎤 disable khi chưa chọn tiểu mục nào.
- **AI sinh on-demand, không pre-generate (user chốt 16/07):** quiz/card/pre-questions chỉ được AI tạo tại thời điểm user bấm hành động lần đầu cho tiểu mục đó (1 call Gemini đúng scope tiểu mục) rồi cache vào DB — tuyệt đối không sinh trước hàng loạt cho cả handbook (23+ call lãng phí, vỡ quota free tier). UI có trạng thái "AI đang tạo đề…" (đã mock 2s trong prototype). Làm lại quiz dùng đề đã cache (0 call); nút "Đổi đề mới" (nếu làm sau này) mới tốn call.
- **Điều phối có ngữ cảnh (user chốt 16/07):** mọi hành động trong checklist tiểu mục khi nhảy sang tab khác PHẢI mang theo ngữ cảnh tiểu mục — không nhảy tab chung chung. Quiz → highlight + auto-scroll đúng section (ring primary + nhãn "quiz của tiểu mục bạn vừa chọn") kèm nút "Xem lại" (mở bài cũ + nhận xét) đứng cạnh "Làm lại". Flashcard → chế độ duyệt bộ card của đúng tiểu mục (lật xem từng card, nút "Ôn bộ card này", link về phiên ôn chung). Feynman → scope đặt theo tiểu mục + phiên cũ của tiểu mục được highlight + mở sẵn trong nhật ký. Cả dòng checklist là nút bấm (hit-target lớn), không chỉ nút nhỏ bên phải. Spec: GoTab(tab, ctx {sectionId | unitKey}).
- **Xem lại & làm lại (user chốt 16/07):** mọi bài quiz/Feynman đều xem lại được đầy đủ (bài làm + nhận xét AI), sau đó tùy chọn làm lại. Lịch sử attempt là append-only — làm lại KHÔNG xóa lần cũ, điểm tốt nhất dùng cho checklist/mastery. UI: chip điểm cũ trong tab Kiểm tra click được → màn xem lại (đáp án đã chọn, giải thích, nhận xét tự luận, trích đoạn) → nút "Làm lại bài này"; nhật ký Feynman có nút "Giảng lại tiểu mục này". Spec backend: `quiz_attempts` lưu cả `answers` JSON (không chỉ điểm) để render lại bài.
- **Checklist per tiểu mục (user đề xuất 15/07, đã đồng thuận):** mỗi tiểu mục x.y.z có checklist 4 việc cụ thể — ① Đọc bài ② Quiz ≥ 80% ③ Tạo flashcard ④ Giảng lại Feynman. Đủ 4 = Vững 🟢; đây chính là mastery formula hiển thị tường minh. Click dòng tiểu mục = bung checklist (không mở doc trực tiếp nữa); mỗi việc có nút hành động riêng (Đọc/Làm quiz/Tạo card/Giảng). 4 icon mini trên dòng thu gọn để nhìn nhanh. Unit 🔴 decayed hiện banner "làm lại quiz để xanh trở lại". Việc "ôn flashcard định kỳ" KHÔNG nằm trong checklist (là vòng lặp vô hạn theo lịch, thuộc tab Ôn tập).
