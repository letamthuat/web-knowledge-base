// Settings is rendered by AppShell at layout level (keep-alive).
// This page module exists only so Next.js recognises the /settings route.
// AppShell intercepts the route and renders SettingsPageInner directly.
export default function SettingsPage() {
  return null;
}
