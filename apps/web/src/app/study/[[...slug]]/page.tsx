// Study is rendered by AppShell at layout level (keep-alive).
// Optional catch-all so mọi deep-link của module đều là route hợp lệ khi hard-reload:
// /study · /study/:spaceId · /study/:spaceId/:tab (plan|review|quiz|feynman)
export default function StudyPage() {
  return null;
}
